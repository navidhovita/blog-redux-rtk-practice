import { createTheme } from "@mui/material/styles";

const theme = createTheme({
    palette: {
        primary: {
            main: "#434ce5",
            dark: "#212121",
            
        },
        // black: "#212121",
        // lightBlack: "#525252"
        // primary: blue,
        // secondary: pink,
    }
} as any)

export default theme;